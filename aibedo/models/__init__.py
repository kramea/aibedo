"""
:mod:`aibedo.models` is a package implementing various ML models.
The interface is general enough, so that new or extended models can be easily integrated into the framework.
"""

from .base_model import BaseModel